'use strict';

/**
 * extended-characteristic controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::extended-characteristic.extended-characteristic');
