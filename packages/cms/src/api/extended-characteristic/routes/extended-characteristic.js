'use strict';

/**
 * extended-characteristic router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::extended-characteristic.extended-characteristic');
