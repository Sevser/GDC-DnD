'use strict';

/**
 * base-characteristic controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::base-characteristic.base-characteristic');
