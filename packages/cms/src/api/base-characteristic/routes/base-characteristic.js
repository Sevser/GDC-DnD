'use strict';

/**
 * base-characteristic router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::base-characteristic.base-characteristic');
